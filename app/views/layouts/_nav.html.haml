%nav
  %ul
    %li=link_to 'Home', root_path
    %li=link_to 'Contact us', contact_infos_path
    - if user_signed_in?
      %li=link_to "Hello, #{current_user.name}", "#"
      %li=link_to 'Logout', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?' }
    - else 
      %li.active=link_to 'Login', new_user_session_path